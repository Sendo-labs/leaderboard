On October 18, 2025, the Sendo project saw the foundational setup of the `sendo-api` repository, establishing its core architecture and crucial transaction decoding utilities. This significant initial commit lays the groundwork for analyzing on-chain history, while the `reusable-workflows` and `frontend` repositories had no new activity.

## ✅ Completed Work
### Project Initialization and Core Structure
*   The `sendo-api` repository was initialized with its foundational architecture, including configuration files, controllers for NFTs, tokens, and trades, and essential service integrations. ([Sendo-labs/sendo-api#1](https://github.com/Sendo-labs/sendo-api/pull/1))
*   Utility functions for decoding various on-chain transactions from platforms like Jupiter, Meteora, Orca, Pumpfun, Pumpswap, Raydium, and Whirlpool were developed and integrated into the `sendo-api`. ([Sendo-labs/sendo-api#1](https://github.com/Sendo-labs/sendo-api/pull/1))